# Under normal circumstances, this file would be ignored by git, but by the nature of this project, we have to track this file.

ATLAS_URI="mongodb://localhost:27017/"   # Probably something like mongodb://localhost:27017/
PORT=3001   # Port you want the server to run. The requirements specify port 3001.